name: Build Route96 Umbrel image

on:
  push:
    tags:
      - "route96-v*"

permissions:
  contents: read
  packages: write

concurrency:
  group: route96-image-${{ github.ref }}
  cancel-in-progress: false

env:
  UPSTREAM_REPOSITORY: v0l/route96
  UPSTREAM_COMMIT: 3429578e0cd9746431a1cc925edcdd21405233d3

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Validate release tag
        id: version
        shell: bash
        run: |
          version="${GITHUB_REF_NAME#route96-v}"
          if [[ "$version" != "0.7.0-3429578" ]]; then
            echo "Unsupported Route96 release tag: $GITHUB_REF_NAME" >&2
            exit 1
          fi
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Check out the exact upstream commit
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          repository: ${{ env.UPSTREAM_REPOSITORY }}
          ref: ${{ env.UPSTREAM_COMMIT }}
          path: upstream
          persist-credentials: false

      - name: Verify upstream commit identity
        shell: bash
        run: |
          actual_commit="$(git -C upstream rev-parse HEAD)"
          test "$actual_commit" = "$UPSTREAM_COMMIT"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@c7c53464625b32c7a7e944ae62b3e17d2b600130

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and publish the Umbrel image
        id: build
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8
        with:
          context: ./upstream
          file: ./upstream/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ghcr.io/whirmill/route96:v${{ steps.version.outputs.version }}
          labels: |
            org.opencontainers.image.source=https://github.com/${{ env.UPSTREAM_REPOSITORY }}
            org.opencontainers.image.revision=${{ env.UPSTREAM_COMMIT }}
            org.opencontainers.image.version=v${{ steps.version.outputs.version }}
          provenance: mode=max
          sbom: true
          cache-from: type=gha,scope=route96
          cache-to: type=gha,mode=max,scope=route96

      - name: Record immutable image digest
        shell: bash
        run: |
          {
            echo "### Route96 Umbrel image"
            echo '```text'
            echo "ghcr.io/whirmill/route96:v${{ steps.version.outputs.version }}@${{ steps.build.outputs.digest }}"
            echo '```'
            echo "Upstream commit: $UPSTREAM_COMMIT"
          } >> "$GITHUB_STEP_SUMMARY"
