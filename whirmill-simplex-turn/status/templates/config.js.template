/* Runtime configuration generated by nginx envsubst. Do not log these values. */
window.SIMPLEX_TURN_CONFIG = Object.freeze({
  host: "${TURN_HOST}",
  port: "${TURN_PORT}",
  username: "${TURN_USERNAME}",
  password: "${TURN_PASSWORD}"
});
